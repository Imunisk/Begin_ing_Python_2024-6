# in 을 활용해
# 키가 존재하는지 확인하고 값에 접근하기
# 사용자로부터 입력을 받음

dic = {
    "name" : "매일 동결건조 두리안",
    "net_weight" : "210g",
    "best_before" : “12개월,
    "origin" : "필리핀"
}
key = input("> 접근하고자 하는 키: ")

# 출력
if key in dic:
    print(dic[key])
else:
    pass
    #print("Error is made.")

#  > 접근하고자 하는 키: origin
# 필리핀
